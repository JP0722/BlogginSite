<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Register</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <div class="register-header">
      <h2>BloggingSite</h2>
    </div>
    <div class="login-div">
      <h4>Register Here</h4>
      <form action="/register" method="POST" class="form-signin">
          <div class="form-group form-inline">
            <label for="email">Username</label>
            <input type="text" class="form-control" name="username"
              pattern="[a-zA-Z0-9]{4,10}" title="Must only contains either lowercase or uppercase or digits, and length should be 4 to 10 characters"
              required
            >
          </div>
          <div class="form-group">
            <label for="password">Password</label>
            <input type="password" class="form-control" name="password"
              pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{5,}"
              title="Must contain at least one  number and one uppercase and lowercase letter, and at least 5 or more characters"
              required>
          </div>
          <button type="submit" class="btn btn-dark form-sbmt-btn">Register</button>
      </form>
      <% if(errMessage.length>0) { %>
          <p style="color: red;"><%= errMessage[0].message %></p>
      <% } %>
    </div>
  </body>
</html>
